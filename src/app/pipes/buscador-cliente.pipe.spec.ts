import { BuscadorClientePipe } from './buscador-cliente.pipe';

describe('BuscadorClientePipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorClientePipe();
    expect(pipe).toBeTruthy();
  });
});
